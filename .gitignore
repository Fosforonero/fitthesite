# Dependencies
/node_modules
/.pnp
.pnp.js

# loctron nordic wave scratch files (extraction/translation intermediates)
.wave2-*
.landing-es-*
*.bak
.yarn/install-state.gz

# Testing
/coverage

# Next.js
/.next/
/out/

# Production
/build

# Misc
.DS_Store
*.pem

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Local env files
.env*.local
.env
!.env.example

# Vercel
.vercel

# TypeScript
*.tsbuildinfo
next-env.d.ts

# Supabase
.supabase
supabase/.temp
supabase/.branches
supabase/seed.local.sql
.pnpm-store/
.claude/

# P1.1 Fase 7: vettori oracle generati (rigenerati a ogni check, non da committare)
.oracle-vectors.json

# Python oracle (P1.1 Fase 7): bytecode cache, mai da committare
__pycache__/
*.pyc

# P1.1B Fase 6: un solo lockfile operativo (pnpm-lock.yaml). Il progetto usa
# pnpm ovunque (Vercel installCommand, Docker, questo repo) - se un comando
# npm locale rigenera package-lock.json per errore, non deve rientrare nel
# repo (successo esattamente questo in una sessione precedente).
package-lock.json
