/**
 * Icona admin Payload (nav compatta) — monogramma FM di FitMesh Sync.
 * Sostituisce l'icona Payload di default.
 */
export const Icon = () => (
  // eslint-disable-next-line @next/next/no-img-element
  <img
    src="/icon-square-128.png"
    alt="FitMesh Sync"
    width={26}
    height={26}
    style={{ borderRadius: 6, display: 'block' }}
  />
);

export default Icon;
