# SEO daily check — 2026-05-27 (run MANUALE)

> Eseguito a mano (la pipeline SEO-ops automatica del README non è ancora
> implementata: niente route `/api/internal/seo-ops/*`, niente agenti nel
> codice — solo `SEO-OPS-README.md` come design). Questo è il check che
> avrebbe fatto lo SEOWatchAgent, per le parti possibili senza credenziali.

## Sitemap & crawl
- **Sitemap** `https://www.fitmesh.fit/sitemap.xml`: HTTP 200, 42 KB, **88 URL**.
- **Link rotti**: **0 / 88** (HEAD+follow su ogni URL → tutti 200). ✅
- **robots.txt**: corretto — `Allow: /`, `Disallow` su `/app/ /admin/ /auth/ /oauth/ /api/`, `Host` + `Sitemap` dichiarati. ✅

## On-page (home /it)
- `<title>`: "FitMesh Sync — Sincronizza il tuo smartwatch a una dashboard personale" ✅
- meta description: presente, ~155 char ✅
- canonical: `https://www.fitmesh.fit/it` ✅
- hreflang: 5 tag (it/en/x-default) ✅
- og:title: presente ✅
- JSON-LD: **6 blocchi** structured data ✅
- `<h1>`: esattamente 1 ✅

## NON verificabile a mano (serve setup)
- **Core Web Vitals**: PageSpeed Insights richiede `GOOGLE_PSI_API_KEY` (quota keyless esaurita). Non misurato.
- **Ranking / impression / CTR (GSC)**: nessuna credenziale Search Console disponibile in sessione. Da confermare che GSC sia verificato + `GSC_SERVICE_ACCOUNT_JSON` settato.

## Note / gap
- La pipeline SEO-ops (monitor GSC, auto-content, alert) è **solo un design doc**, non implementata. Per attivarla servono: implementare le route + agenti, settare le env (GSC/PSI/GitHub token), e schedularla **via GitHub Actions** (i 2 slot cron Vercel Hobby sono pieni: welcome-emails + indexnow).
- IndexNow daily è l'unica automazione SEO realmente attiva.

## Verdetto
**SEO tecnico on-site: sano.** 0 link rotti, metadati/structured data/hreflang a posto, sitemap pulita. I gap sono di *osservabilità* (CWV + GSC non monitorati) e *content automation* (pipeline non implementata), non di igiene tecnica.
