# ============================================================================
# Perimetro della suite Vitest — congelato il 26/08/2026 (INFRA-5).
#
# PERCHE'
# -------
# Il 25/08/2026 la suite raccoglieva 6.478 file, 6.444 dei quali provenivano
# da checkout di altri rami dentro `.claude/worktrees/`. Un cancello di
# release che dice «verde» misurando in maggioranza altri rami non misura
# niente. Questi due numeri esistono perche' una deriva si veda, in ENTRAMBE
# le direzioni: file in piu' vuol dire che il perimetro si e' riaperto, file
# in meno vuol dire che dei test hanno smesso di essere eseguiti — che e' il
# modo piu' silenzioso di rompere una suite.
#
# COME SI AGGIORNANO
# ------------------
# A mano, in un commit che si vede, quando si aggiungono o tolgono test di
# proposito. Mai per far tornare verde un gate rosso senza aver capito perche'.
# ============================================================================
# 01/09/2026 — copertura-privacy.test.ts, 55 test.
#   La privacy policy pubblicata non nominava identificatori, cronologia
#   acquisti, contenuti scritti dall'utente, Firebase Installations e
#   OpenStreetMap. Il test tiene la policy agganciata all'inventario di
#   FASE-6-PRIVACY-IOS.md, e lo fa in TUTTE e sei le lingue: una policy vera in
#   italiano e incompleta in tedesco e' incompleta per chi legge in tedesco.
#   45 -> 46 file, 768 -> 823 test.

# ── Sync di PR #61/#62/#63 con main, 01/09/2026 (voce unica dopo rebase) ────
# Le tre PR erano aperte prima del 26/08/2026 (INFRA-5), mai risincronizzate.
# #61 (hotfix/google-health-ja-ko-unicode-escape) ha sincronizzato per prima
# ed e' stata la prima mergiata in main (a1ec92b): main porta gia' file=50,
# test=908 da quel commit. #62 ha fatto rebase su quel main, aggiungendo i
# propri +5 test (OutboundTracker.test.tsx, contratto cta_view) e portando
# main a file=50/test=913 (cfe54f5, seconda PR mergiata). Questo branch
# (PR #63) fa rebase sul main post-#62: 46 -> 50 file (+4), stessi 4 file
# gia' spiegati dal commit di #61 (git-diff dei file di test fra l'ultimo
# freeze, d841d42, e main):
#   +1 components/LanguageSwitcher.test.tsx (10 test, PR #59)
#   +1 lib/blog/language-switcher-ssot.test.ts (4 test, PR #59)
#   +1 lib/site-url.test.ts (3 test, PR #59)
#   +1 lib/providers/indexability.test.ts (4 test, PR #58)
# Questo ramo non aggiunge di suo nessun file *.test.ts (il lavoro di PR #63
# e' editoriale/locale, in tools/check-p19b-ring-locales.ts che non e' un
# file di test tracciato da questo perimetro) ne' nuovi casi a file
# esistenti — verificato con `git diff origin/main..HEAD -- '*.test.ts'
# '*.test.tsx'`, vuoto. I numeri restano quindi IDENTICI a quelli gia' in
# main dopo #62, nessun aggiornamento necessario oltre a questa voce.
# Rieseguito `bash tools/gate-release-suite.sh --costruisci` in locale DOPO
# il rebase su main (cfe54f5): verde, 0 fallimenti, PG17 effimero distrutto
# e verificato.
#   823 -> 913 test (modalita' normale). saltati invariato (21).

# ── Deriva pre-esistente, non di questo ramo, 04/09/2026 ─────────────────────
# Worktree P0.18A (content/p018a-privacy-containment) creato da origin/main
# (e975c78) per un hotfix privacy isolato: zero file *.test.ts toccati da
# questo ramo (`git diff origin/main..HEAD -- '*.test.ts' '*.test.tsx'`
# vuoto). Il gate risultava comunque rosso: 915 raccolti contro 913
# congelati. Isolato a app/api/v1/billing/validate-purchase/route.db.test.ts,
# 17 -> 19 test: stessa deriva gia' diagnosticata e congelata su PR #66
# (worktree fitthesite-p1910-steps-truth) il 01-02/09/2026, causata da un
# workstream billing gia' mergiato su main (3868d6b/662275a) che non aveva
# mai aggiornato questo file. Qui si applica la stessa correzione, non una
# nuova: nessuna modifica di questo ramo l'ha causata.
#   913 -> 915 test (modalita' normale). saltati invariato (21).
#
# 04/09/2026 — MICRO-GATE P0.18A-B: +1 test in lib/blog/language-switcher-
# ssot.test.ts, aggiunto insieme al ritiro temporaneo (redirect 307) del
# post "dove-sono-i-tuoi-dati-server-ue". Un test preesistente asserisce
# "IT e' sempre indicizzabile per ogni post" — invariante generale corretta,
# rotta di proposito per questo UNICO post (vedi WITHDRAWN_PENDING_APP_MATRIX_
# VARIANTS in lib/blog/indexability.ts). Aggiunta un'eccezione esplicita e
# documentata al test esistente (non rimosso l'assert, solo escluso questo
# slug) + un nuovo test dedicato che verifica il ritiro completo su tutte le
# 15 locale. Stesso file, nessun nuovo file: 50 -> 50.
#   915 -> 916 test (modalita' normale). saltati invariato (21).
#
# 08/09/2026 — REBASE PR #66 su main post-P0.18A-B (MICRO-GATE PR#66-B). Le
# +6 di copertura-privacy.test.ts (correzione OSM/GPS) sono state RIMOSSE
# durante la risoluzione dei conflitti: quella disclosure resta rimandata a
# P0.18 (MATRIX_COMPLETE_FOR_SITE = NO), quindi il file e' tornato identico a
# origin/main — zero test in piu' da li'. Il +2 di route.db.test.ts era gia'
# incluso nel 916 sopra (stessa deriva pre-esistente, non ricontata due
# volte). Resta solo il +1 genuino di questo branch: lib/blog/language-
# switcher-ssot.test.ts scala da solo (parametrizzato su BLOG_POSTS) con
# l'aggiunta del nuovo post steps-total-vs-hourly-chart.ts — nessuna modifica
# al file di test stesso, solo un'iterazione in piu'. Numero verificato
# rieseguendo il gate reale dopo il rebase, non calcolato a mano.
#   916 -> 917 test (modalita' normale). saltati invariato (21).
#
# 08/09/2026 (stesso giorno) — pubblicazione /novita 190 (punto 7 del
# mandato, lib/blog/posts/novita-passi-piu-affidabili.ts): stesso scaling
# automatico spiegato sopra, sul nuovo post stesso. Nessun nuovo file di
# test, nessuna modifica a un file di test esistente.
#   917 -> 918 test (modalita' normale). saltati invariato (21).
#
# 10/09/2026 — pubblicazione della voce /novita 3.10.0/191 (lib/blog/posts/
# novita-giorni-passati.ts): stesso scaling automatico di language-
# switcher-ssot.test.ts sul nuovo post stesso. Nessun file di test nuovo o
# modificato. Numero verificato rieseguendo il gate reale, non calcolato a
# mano.
#   918 -> 919 test (modalita' normale). saltati invariato (21).
#
# 14/09/2026 — SPRINT P1.22-B, PR #73 (lib/compatibility/matrix.test.ts):
# +1 file di test con 12 guardrail su compatibilita', SSOT slug, separazione
# OS, 9 percorsi, sottopercorsi Samsung e glossario essenziale.
#   50 -> 51 file, 920 -> 932 test (modalita' normale). saltati (23).
#
# 14/09/2026 (stesso giorno) — MICRO-GATE P1.22-B-A: +3 test aggiuntivi in
# lib/compatibility/matrix.test.ts per: 1) guardrail negativo permanente su
# Colmi iOS (zero write-back di sonno a Apple Health con kHealthKitSleepExportEnabled=false,
# divieto di "write-back certificato" / "senza cloud terzi", architettura FitMesh
# e privacy policy); 2) test anti-leak di stringhe italiane in EN/DE/FR in UI_COPY;
# 3) test su link interni SSOT (providerLinkHref + blogLinkHrefSync) con tag (EN)
# esplicito per varianti non tradotte (Oura FR). Nessun nuovo file: 51 -> 51.
#   932 -> 935 test (modalita' normale). saltati invariato (23).
#
# 14/09/2026 (stesso giorno) — MICRO-GATE P1.22-B-B: +4 test aggiuntivi in
# lib/compatibility/matrix.test.ts per: 1) guardrail negativo permanente su
# Oura iOS (zero esportazione resting HR / RESTING_HEART_RATE ad Apple Health
# senza evidenza diretta, riconciliazione frequenza respiratoria); 2) guardrail
# Oura Android per distinzione netta tra frequenza cardiaca e HRV RMSSD;
# 3) coerenza dominio/titolo fonti ufficiali e Apple Developer per Colmi iOS;
# 4) etichetta sourceDocumentsLabel senza virgolette sui claim parafrasati nel
# componente. Nessun nuovo file: 51 -> 51.
#
# 15/09/2026 — SPRINT P0.22-C: +1 file (lib/providers/catalog-coherence.test.ts)
# e +16 test di guardrail per la riconciliazione catalogo e matrice integrazioni:
# 1) Oura: status 'live-bridge', platforms ['android', 'ios'], syncMechanism 'system-bridge',
#    esclusione di distanza e resting HR autonomo su Oura iOS, nessun claim di API diretta live o
#    ETA su Oura, authentic localization FR e KO per Oura;
# 2) Fitbit: rimozione viaHC, rimozione Q3/T3 2026, 12-month backfill e promesse OAuth;
# 3) Garmin: rimozione viaHC, rimozione Q3/T3 2026, precisazione approvazione formale
#    Garmin Developer Program, authentic localization PL;
# 4) Coerenza Product Facts (LIVE_STATUSES con live-bridge, Oura presente in ANDROID/IOS);
# 5) Coerenza landing pages (oura-ring-sync, fitbit-export-google, garmin-connect-pc) e about-copy.
#   51 -> 52 file, 939 -> 955 test (modalita' normale). saltati invariato (23).
#
# 15/09/2026 (stesso giorno) — MICRO-GATE P0.22-C-A: +8 test aggiuntivi in
# lib/providers/catalog-coherence.test.ts per: 1) guardrail modelli Oura
# (ring-gen3 e ring-4) su presenza platformFeatures distinte, divieto metriche
# non documentate su Health Connect (SpO2, temperatura cutanea, resting HR,
# sleep stages, RMSSD) e separazione a 4 livelli in 11 lingue; 2) completezza
# dizionario PLATFORM_FEATURE_LABELS su tutte le lingue senza fallback;
# 3) SSOT providerPlatforms per visualizzazione selettiva store buttons (Android-only
# esclude store iOS). Nessun nuovo file: 52 -> 52.
#   955 -> 963 test (modalita' normale). saltati invariato (23).
#
# 15/09/2026 (stesso giorno) — MICRO-GATE P0.22-C-B: +16 test aggiuntivi in
# lib/providers/catalog-coherence.test.ts per: 1) Huawei Health con status neutro
# 'not-available', zero metriche supportate, nessuna setupGuide, rimozione totale
# di date Q4, waitlist, beta, polling, backfill, OAuth e bonifica marketing out-of-the-box;
# 2) Suunto con status neutro 'not-available', zero metriche supportate (workouts=false),
# divieto fake claims (PKCE, webhook, tempo reale, sync apertura, tempi arbitrari,
# route interne leakate); 3) Invariante universale 'not-available' (zero metriche
# supportate, zero setupGuide, zero viaHC su tutti i provider non disponibili).
# 16/09/2026 — MICRO-GATE PR #74 / P0.22-C-E: lib/content/famiglia-ssot.test.ts (10 test)
# e lib/providers/catalog-coherence.test.ts (+1 test guardrail negativo: divieto Hanja 수面,
# divieto all health data Oura, divieto reads locally, divieto latenze numeriche 15-30 e
# divieto confronto sensori Oura Ring 4).
# 52 -> 53 file (+1), 979 -> 990 test (+11). saltati invariato (23).
# 16/09/2026 (stesso giorno) — MICRO-GATE PR #74 / P0.22-C-F: lib/providers/catalog-coherence.test.ts (+3 test
# guardrail semantici: eliminazione assoluti negativi Oura su metriche proprietarie e verifica copy positivo,
# divieto fully functional / all supported models / partnership Garmin / bridge automatico incondizionato,
# divieto 'Android Health Connect' a favore di 'Health Connect su Android' nelle FAQ modificate).
# 53 file invariato, 990 -> 993 test (+3). saltati invariato (23).
# 16/09/2026 (stesso giorno) — MICRO-GATE PR #74 / P0.22-C-G: lib/providers/catalog-coherence.test.ts (+3 test
# guardrail: requisito Android 14+ setupGuide Garmin e divieto Android 10-13 sufficiente, divieto consigliato/recommended
# nelle FAQ membership Oura Gen3/4 ed enforcement requisito fattuale, divieto HRV in elenco congiunto multipiattaforma Oura).
# 53 file invariato, 993 -> 996 test (+3). saltati invariato (23).
# 17/09/2026 — SPRINT P0.24-A (privacy analytics): tre file nuovi (il terzo dal micro-gate P0.24-A-A).
#   components/AnalyticsConsent.test.tsx (82 test): nessuno script GA, gtag o
#   dataLayer prima del consenso; rifiuto senza caricamento ne' ricarica; un
#   solo caricamento e un solo config dopo il consenso; navigazione client-side
#   senza page_view aggiunti; revoca che ferma gli eventi, cancella i cookie
#   _ga e ricarica la pagina senza il tag; fino alla ricarica fetch e
#   sendBeacon verso GA e Google tag scartati; scelta propagata alle altre
#   schede; page_location della pagina corrente su ogni evento; GA sospeso
#   prima che gtag.js veda un pushState o un popstate verso una route esclusa;
#   link degli store leggibili; route con codici nell'URL mai caricate;
#   referrer di una route esclusa mai inviato a GA; parametri evento filtrati.
#   Micro-gate P0.24-A-A (+43): dl e dr (e i campi campagna) di ogni richiesta
#   di raccolta ripuliti all'uscita, perche' i page_view generati da gtag.js
#   sulla history portano la query grezza; valori UTM non sicuri; pageshow dopo
#   un ripristino dalla cache avanti/indietro; percorsi esclusi indipendenti
#   dalla normalizzazione dell'hosting; banner sincronizzato tra schede;
#   wrapper di fetch e sendBeacon fedeli al comportamento nativo; scelta
#   valida per il documento con lo storage indisponibile; cancellazione della
#   famiglia di cookie GA su host, domini padre e percorsi (+9 in questo passaggio).
#   lib/analytics/privacy-copy.test.ts (17 test): nessuna negazione di Google
#   Analytics nelle 15 lingue; About, Cookie Policy, Privacy Policy e banner
#   coerenti col comportamento (ricarica senza il tag compresa); unico loader
#   in lib/analytics/consent.ts; landing Fitbit senza cancellazione totale,
#   copia promessa, Ace e configurazione in 5 minuti (R28).
#   components/OutboundTracker.test.tsx: stesso numero di test; il setup ora
#   dichiara lo stato «consenso dato e GA caricato» e page_location e' tra i
#   parametri ammessi.
#   app/(frontend)/[locale]/(marketing)/legal-dates-parity.test.tsx (36 test,
#   micro-gate P0.24-A-A): Cookie Policy e Privacy Policy renderizzate nelle 15
#   lingue; la data visibile, il dateModified del JSON-LD e le eventuali date
#   nei metadata dicono lo stesso giorno, qualunque sia il valore.
# 53 -> 56 file (+3), 996 -> 1131 test (+135). saltati invariato (23).
#
# 22/09/2026 — MICRO-GATE P0.24-A-A, seconda revisione indipendente: stesso
# +16 spiegato sopra (test_release) anche in modalita' non-release. Nessun
# file nuovo, saltati invariato.
#   1131 -> 1147 test.
#
# 22/09/2026 — SPRINT P0.24-B FASE A: fact lock Family Mesh. /llms.txt e le
# feature list di lib/product-facts.ts (quest'ultime codice morto: l'unico
# consumatore, MobileApplicationJsonLd, non e' montato su nessuna pagina)
# descrivevano Family Mesh come funzione disponibile, con dettagli di
# comportamento inventati; /famiglia dice correttamente che il progetto e' in
# valutazione, nessuna data. tools/check-llms-consistency.ts non se ne
# accorgeva (verificato rosso->verde sul testo vecchio prima della correzione).
# +2 file nuovi (lib/llms-txt.test.ts 3 test, lib/product-facts.test.ts 1
# test), entrambi agganciati a getFamigliaComingSoon() come fonte di verita',
# non a una frase congelata.
#   56 -> 58 file (+2), 1147 -> 1151 test (+4). saltati invariato (23).
#
# 22/09/2026 (stesso giorno) — RETTIFICA: decisione di prodotto confermata da
# Matteo, Mesh Famiglia e' decisa ed e' in sviluppo (non "in valutazione",
# che sottostimava). lib/llms-txt.test.ts +1 test: l'ancora indipendente
# CAPABILITY_STATUS.familyMesh in lib/product-facts.ts, cosi' il fact lock non
# dipende solo dal confronto fra /llms.txt e /famiglia (provato: una modifica
# scorretta coordinata a entrambe le pagine, senza toccare l'ancora, resta
# rossa). Nessun file nuovo, saltati invariato.
#   1151 -> 1152 test.
#
# 22/09/2026 (stesso giorno) — file nuovo family-mesh-parity.test.tsx (16
# test: 1 precondizione + 15 lingue), stesso schema di legal-dates-parity.test.tsx
# (P0.24-A-A): renderizza /famiglia con renderToStaticMarkup e controlla che
# testo visibile, metadata e JSON-LD (WebPage + FAQPage) dicano tutti "in
# sviluppo", in tutte le 15 lingue, senza formule superate ne' dettagli di
# funzionamento non verificati (gruppo, inviti, prezzo, tetto membri).
#   58 -> 59 file (+1), 1152 -> 1168 test (+16). saltati invariato (23).
file=59
test=1168
saltati=23

# ── Deriva del 26/08/2026, di proposito (separazione delle autorita') ────────
# 43 -> 44 file, 742 -> 757 test. Cosa e' cambiato:
#   -1 file  test/legacy-189/vendored.test.ts, che confrontava la copia con
#            `main` — un ref che si muove, e che infatti si era mosso.
#   +1 file  test/billing-route-fixtures/manifesto.test.ts (8 test): impronta,
#            provenienza dal commit COMPLETO, e il guardrail che vieta a
#            chiunque di prendere un branch per testimone.
#   +1 file  app/api/v1/billing/validate-purchase/route.storica.db.test.ts
#            (3 test): la suite DIAGNOSTICA sulla copia at-release.
#   +5 test  in route.db.test.ts, che passa da 5 a 10: la matrice ora ha la
#            cella che non c'era, cioe' il client letterale 189 contro la
#            candidata 190.
#   +2 test  in test/ci-backend.test.ts: le dichiarazioni di Node e il divieto
#            di ref mobili nel workflow.
#   -3 +8 +3 +5 +2 = +15.
#
# 26/08, secondo scatto: 757 -> 764. L'invariante era UN test che ciclava
# 2 client x 4 scenari; sul runner GitHub sforava i 5 s di timeout predefinito
# di vitest. Adesso sono OTTO casi nominati (+7). Non un timeout alzato: lo
# stesso lavoro diviso per otto, ognuno sotto i 700 ms.
#
# `saltati` passa da 6 a 14 perche' i test contro il database sono ora 13 su
# due file (10 + 3) invece di 5 su uno, piu' l'unico skip autorizzato.

# ── Deriva del 28/08/2026, di proposito (lavoro NON-3B, dichiarato) ─────────
# 44 -> 46 file, 764 -> 780 test. Cosa e' cambiato, e perche' non e' una
# riapertura del perimetro:
#   +1 file  app/api/cron/sync-trigger/payload.test.ts (11 test)
#   +1 file  app/api/cron/sync-trigger/route.test.ts   (5 test)
# Sono atterrati con `3874431` (BG1-1, il push di sincronizzazione che sveglia
# anche iOS): lavoro di un altro filone, arrivato DOPO il congelamento del
# 26/08 e non dichiarato qui. Il gate e' andato rosso per questo, ed e'
# esattamente il suo mestiere — la suite passava (780/780, un solo skip
# autorizzato), a non tornare erano i numeri attesi.
#
# Trovato dalla review indipendente del filone billing, il 28/08. Registrato
# qui invece che nel referto perche' la causa non e' billing: e' che chi ha
# aggiunto quei test — io — non ha aggiornato il registro. Il cancello ha
# funzionato.

# ── Deriva del 31/08/2026, di proposito (esclusione del perimetro billing) ──
# 46 -> 44 file, 780 -> 760 test. Cosa e' uscito, e perche' non e' un gate
# fatto tornare verde:
#   -1 file  app/api/v1/billing/notifications/apple/route.test.ts (10 test)
#   -1 file  lib/billing/pubsub-push.test.ts                      (10 test)
#
# I due file testavano i due canali asincroni — App Store Server Notifications
# V2 e Google RTDN — usciti dalla 190 come unita' indivisibile insieme a F5 e
# F6. Non e' un test che smette di girare: e' il codice sotto il test che non
# viene spedito. Nessuna delle due route esiste piu' nel ramo, e nessuna delle
# due e' mai stata servita in produzione (404 misurato il 28/08; il 405 su
# validate-purchase era il controllo positivo della sonda).
#
# `saltati` resta 21 e `saltati_release` resta 1: nessuno dei due file
# rimossi conteneva skip.
#
# Cosa NON e' uscito, e va detto perche' e' l'errore facile da fare leggendo
# l'elenco dei file per nome: `lib/billing/apple-root-ca.ts` sta nella stessa
# cartella e sembra del canale asincrono. Non lo e'. E' il trust store dei
# root Apple, condiviso con `app-store-jws.ts`, cioe' con il verificatore
# StoreKit 2 del percorso sincrono — il fix del P0 Apple. Se fosse uscito, il
# percorso sincrono non compilerebbe e il P0 sarebbe uscito con lui.

# ── Deriva del 31/08/2026, seconda (M17, ancoraggio della catena JWS) ───────
# 44 -> 45 file, 760 -> 768 test. Cosa e' entrato:
#   +1 file  lib/billing/app-store-jws-ancoraggio.test.ts (8 test)
#
# Chiude M17: fino a oggi nessun test dimostrava che i byte dei root Apple
# arrivassero dentro `SignedDataVerifier` e vi facessero qualcosa. Il caso
# negativo esistente usava `x5c: []`, che inciampa nel controllo di lunghezza
# della catena molto prima dell'ancoraggio, quindi una catena BEN FORMATA e
# autofirmata da chiunque sarebbe stata accettata con la suite verde.
#
# Il file porta una catena sintetica vera (radice, intermedio, foglia; EC
# P-256, firme reali, e le due estensioni Apple che la libreria pretende) e
# misura che, cambiando SOLO il trust store, il guasto si sposta: con i root
# Apple e' VERIFICATION_FAILURE, cioe' l'ancoraggio; con la propria radice la
# catena passa per intero e si ferma dopo, sull'OCSP.
#
# Due mutazioni provate il 31/08: spegnere `enableOnlineChecks` rende rosso 1
# caso, aggiungere la radice sintetica ai root Apple ne rende rossi 2.

# ── Modalita' release (INFRA-5B) ─────────────────────────────────────────────
# Con il bersaglio del database dichiarato, i sei test contro il database
# girano davvero e questi sono i numeri attesi.
#
# 01/09/2026 — stesso sync spiegato sopra per `file`/`test`: main porta gia'
# test_release=913 dopo il merge di #61 e #62; questo branch (PR #63) non
# aggiunge test propri, stesso totale identico. Confermato con
# `bash tools/gate-release-suite.sh --costruisci` su PG17 effimero locale
# DOPO il rebase su main (cfe54f5): verde, «test saltati al numero
# congelato (1)» — lo skip autorizzato (lib/billing/app-store-jws.test.ts)
# resta l'unico, invariato.
#   768 -> 913 test.
#
# 04/09/2026 — stessa deriva pre-esistente spiegata sopra per la modalita'
# normale (route.db.test.ts 17 -> 19, gia' diagnosticata su PR #66, non
# causata da questo ramo): stesso +2 anche qui, saltati_release invariato.
#   913 -> 915 test.
#
# 04/09/2026 (stesso giorno, MICRO-GATE P0.18A-B) — stesso +1 spiegato sopra
# per la modalita' normale (nuovo test in language-switcher-ssot.test.ts,
# ritiro temporaneo di dove-sono-i-tuoi-dati-server-ue), identico in
# modalita' release: nessun test e' condizionato all'ambiente PG17.
#   915 -> 916 test.
#
# 08/09/2026 — REBASE PR #66 (MICRO-GATE PR#66-B), stesso +1 spiegato sopra
# per la modalita' normale (scaling automatico di language-switcher-ssot.
# test.ts sul nuovo post steps-total-vs-hourly-chart.ts): il totale raccolto
# non dipende dalla modalita', nessun test aggiuntivo e' condizionato
# all'ambiente PG17. saltati_release resta 1.
#   916 -> 917 test.
#
# 08/09/2026 (stesso giorno) — pubblicazione della voce /novita della 190
# (lib/blog/posts/novita-passi-piu-affidabili.ts, punto 7 del mandato):
# stesso scaling automatico di language-switcher-ssot.test.ts, questa volta
# sul nuovo post stesso. Nessun file di test toccato direttamente, nessun
# test condizionato a PG17.
#   917 -> 918 test.
#
# 10/09/2026 — pubblicazione della voce /novita 3.10.0/191 (lib/blog/posts/
# novita-giorni-passati.ts): stesso scaling automatico, sul nuovo post
# stesso. Nessun file di test toccato, nessun test condizionato a PG17.
#   918 -> 919 test.
#
# 11/09/2026 — SPRINT P1.21, PR isolata (lib/blog/posts/nuova-apple-health-
# rende-inutili-altre-app.ts): stesso scaling automatico di
# language-switcher-ssot.test.ts sul nuovo post stesso. Nessun file di test
# toccato, nessun test condizionato a PG17. STOP prima del merge per
# mandato: questa PR resta isolata, nessun deploy/IndexNow senza GO.
#   919 -> 920 test.
#
# 14/09/2026 — SPRINT P1.22-B, PR #73: stesso +12 di matrix.test.ts anche
# in modalita' release (nessun test condizionato a PG17).
#   920 -> 932 test. saltati_release invariato (1).
#
# 14/09/2026 (stesso giorno) — MICRO-GATE P1.22-B-A: stesso +3 spiegato sopra
# per la modalita' normale, identico in modalita' release (nessun test e'
# condizionato a PG17).
#   932 -> 935 test. saltati_release invariato (1).
#
# 14/09/2026 (stesso giorno) — MICRO-GATE P1.22-B-B: stesso +4 spiegato sopra
# per la modalita' normale, identico in modalita' release (nessun test e'
# condizionato a PG17).
#
# 15/09/2026 — SPRINT P0.22-C: stesso +16 di catalog-coherence.test.ts anche
# in modalita' release (nessun test condizionato a PG17).
#   939 -> 955 test. saltati_release invariato (1).
#
# 15/09/2026 (stesso giorno) — MICRO-GATE P0.22-C-A: stesso +8 spiegato sopra
# per la modalita' normale, identico in modalita' release (nessun test e'
# condizionato a PG17).
#   955 -> 963 test. saltati_release invariato (1).
#
# 15/09/2026 (stesso giorno) — MICRO-GATE P0.22-C-B: stesso +16 spiegato sopra
# per la modalita' normale, identico in modalita' release (nessun test e'
# condizionato a PG17).
#   963 -> 979 test. saltati_release invariato (1).
#
# 16/09/2026 — MICRO-GATE PR #74 / P0.22-C-E: stesso +11 spiegato sopra
# anche in modalita' release (nessun test condizionato a PG17).
#   979 -> 990 test. saltati_release invariato (1).
#
# 16/09/2026 (stesso giorno) — MICRO-GATE PR #74 / P0.22-C-F: stesso +3 spiegato sopra
# anche in modalita' release (nessun test condizionato a PG17).
#   990 -> 993 test. saltati_release invariato (1).
#
# 16/09/2026 (stesso giorno) — MICRO-GATE PR #74 / P0.22-C-G: stesso +3 spiegato sopra
# anche in modalita' release (nessun test condizionato a PG17).
#   993 -> 996 test. saltati_release invariato (1).
#
# 17/09/2026 — SPRINT P0.24-A: stesso +135 spiegato sopra anche in modalita'
# release (nessun test condizionato a PG17).
#   996 -> 1131 test. saltati_release invariato (1).
#
# 22/09/2026 — MICRO-GATE P0.24-A-A, seconda revisione indipendente: 16 test
# nuovi in components/AnalyticsConsent.test.tsx (scrittura del consenso che non
# riesce con una scelta vecchia ancora nello storage, ricarica interrotta dopo
# la revoca, filtro dei valori UTM piu' severo, banner risincronizzato dal
# pageshow). Nessun file nuovo, nessun test condizionato a PG17.
#   1131 -> 1147 test. saltati_release invariato (1).
#
# 22/09/2026 — SPRINT P0.24-B FASE A: stesso +4 spiegato sopra (fact lock
# Family Mesh, lib/llms-txt.test.ts + lib/product-facts.test.ts) anche in
# modalita' release (nessun test condizionato a PG17).
#   1147 -> 1151 test. saltati_release invariato (1).
#
# 22/09/2026 (stesso giorno) — RETTIFICA: stesso +1 spiegato sopra (ancora
# CAPABILITY_STATUS.familyMesh) anche in modalita' release.
#   1151 -> 1152 test. saltati_release invariato (1).
#
# 22/09/2026 (stesso giorno) — stesso +16 spiegato sopra (family-mesh-parity.test.tsx)
# anche in modalita' release (nessun test condizionato a PG17).
#   1152 -> 1168 test. saltati_release invariato (1).
test_release=1168
saltati_release=1

# Gli UNICI skip autorizzati, per file. Tutto il resto e' uno skip AMBIENTALE,
# e in modalita' release rende rosso il gate.
#
# lib/billing/app-store-jws.test.ts — la prova positiva con un JWS Apple
#   autentico. Richiede una transazione vera da Sandbox/TestFlight, e un JWS
#   reale non entra in CI ne' nel repository: e' una regola permanente, non una
#   mancanza dell'ambiente. Il test dichiara il salto nel proprio nome.
skip_autorizzato=lib/billing/app-store-jws.test.ts
